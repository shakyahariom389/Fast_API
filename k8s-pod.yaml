apiVersion: v1
kind: Pod
metadata:
  name: fastapi-app
spec:
  containers:
  - name: fastapi
    image: your-image-name
    resources:
      limits:
        memory: "512Mi"
        cpu: "500m"
    ports:
    - containerPort: 8000